{
  // See https://go.microsoft.com/fwlink/?LinkId=733558
  // for the documentation about the tasks.json format
  "version": "2.0.0",
  "tasks": [
    {
      "label": "pnpm build",
      "type": "shell",
      "command": "$SHELL -i -c \"pnpm build\"",
      "problemMatcher": [],
      "group": {
        "kind": "build"
      }
    },
    {
      "label": "pnpm build --continue",
      "type": "shell",
      "command": "$SHELL -i -c \"pnpm build --continue\"",
      "problemMatcher": [],
      "group": {
        "kind": "build"
      }
    },
    {
      "label": "pnpm changeset:add",
      "type": "shell",
      "command": "$SHELL -i -c \"pnpm changeset:add\"",
      "group": {
        "kind": "build"
      }
    }
  ]
}
