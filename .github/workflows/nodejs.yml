name: Node CI
on: [push]
jobs:
  build:
    runs-on: ubuntu-22.04
    timeout-minutes: 12
    if: ${{ !startsWith(github.ref, 'refs/heads/dependabot/npm_and_yarn') }}
    steps:
      - uses: actions/checkout@v3
      - uses: cachix/install-nix-action@v18
      - uses: cachix/cachix-action@v12
        with:
          name: devenv
      - run: nix-env -if https://github.com/cachix/devenv/tarball/v0.5
      - run: devenv ci
      - run: devenv info
      - name: Cache Rush
        uses: actions/cache@v3
        with:
          path: |
            common/temp/install-run
            ~/.rush
          key: ${{ runner.os }}-${{ hashFiles('rush.json') }}
      - name: Cache pnpm
        uses: actions/cache@v3
        with:
          path: |
            common/temp/pnpm-store
          key: ${{ runner.os }}-${{ hashFiles('common/config/rush/pnpm-lock.yaml') }}
      - name: Cache blog/next-web next
        uses: actions/cache@v3
        with:
          path: |
            app/blog/.next/cache/
            examples/next-web/.next/cache/
          key: ${{ runner.os }}-next-${{ hashFiles('common/config/rush/pnpm-lock.yaml') }}
      - name: rush install
        run: |
          devenv shell rush install --bypass-policy
      - name: start db
        run: |
          cd tools/generic-tools/
          devenv up &
          devenv shell npm run db:wait
      - name: setup db
        run: |
          cd tools/generic-tools/
          devenv shell npm run db:ensure-dbs
          devenv shell npm run db:make-users:superuser
          devenv shell npm run db:migrate:all
          devenv shell npm run db:make-users:no-superuser
      - name: rush build
        run: |
          devenv shell rush build --verbose
